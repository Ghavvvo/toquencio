version: "3.9"

services:
  bot:
    build: .
    container_name: toquencio-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    # Uncomment to set a different poll interval at runtime
    # environment:
    #   POLL_SECONDS: 60

